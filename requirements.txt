# v2x_adversarial_sensing — Phase-1 runtime requirements
# All shipped results were produced on CPU with fixed seeds; GPU optional.
torch>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
scikit-learn>=1.3
